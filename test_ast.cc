#include <iostream>
#include "ast.h"

int main() {
  // Expressions
  // NodeExpression exp1, exp2;
  // NodeExpression *exp3;
  // NodeInteger _int(5);
  // NodeFloat _float(5.0);
  // NodeIdentifier var_type("int");
  // NodeIdentifier var_name("a");
  // NodeBinaryOperator binary(exp1, 0, exp2);
  // NodeAssignment assignment(var_name, exp1);

  // // Statements
  // NodeExpressionStatement nes(exp1);
  // NodeVariableDeclaration nvd(var_type, var_name, exp3);
  return 0;
}